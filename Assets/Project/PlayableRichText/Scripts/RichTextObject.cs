using UnityEngine;
namespace PlayableRichText.Scripts
{
    public class RichTextObject : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
